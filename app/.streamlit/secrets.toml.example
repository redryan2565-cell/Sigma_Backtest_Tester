# Streamlit Secrets Template
# Copy this file to .streamlit/secrets.toml and fill in your values
# DO NOT commit secrets.toml to version control

# ============================================
# Security Settings (REQUIRED for production)
# ============================================

# Developer Mode (REQUIRED)
# Set to "false" for production deployment
# Set to "true" only for local development
# When false, Optimization and Leverage Mode tabs are hidden
DEVELOPER_MODE = "false"

# Debug Mode (REQUIRED)
# Set to "false" for production deployment
# Set to "true" only for local development
# When false, detailed error traces are hidden for security
DEBUG_MODE = "false"

# ============================================
# Cache Settings (Optional)
# ============================================

# Enable data caching (default: true)
# Set to "false" to disable caching (not recommended)
CACHE_ENABLED = "true"

# Cache TTL in hours (default: 24)
# How long cached data remains valid
CACHE_TTL_HOURS = "24"

# Cache directory path (optional)
# Defaults to .cache/ in project root if not specified
# CACHE_DIR = ".cache"

# Logging level (optional)
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Defaults to INFO in production, DEBUG if DEBUG_MODE=true
# LOG_LEVEL = "INFO"

# ============================================
# API Keys (Optional)
# ============================================

# Alpha Vantage API Key (optional)
# Required only if using Alpha Vantage data feed
# Get your free API key at: https://www.alphavantage.co/support/#api-key
# ALPHA_VANTAGE_KEY = "your-api-key-here"

# ============================================
# Streamlit Cloud Deployment Notes
# ============================================
#
# For Streamlit Community Cloud:
# 1. Go to your app settings
# 2. Click "Secrets" in the sidebar
# 3. Paste the contents of this file (without the comments)
# 4. Fill in your actual values
# 5. Save and redeploy
#
# Example for Streamlit Cloud Secrets:
# [default]
# DEVELOPER_MODE = "false"
# DEBUG_MODE = "false"
# CACHE_ENABLED = "true"
# CACHE_TTL_HOURS = "24"
#
# ============================================
